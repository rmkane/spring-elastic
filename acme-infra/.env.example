# Copy to .env in this directory (optional). docker compose loads it automatically.
# For interactive shells, prefer:  source ./acme-dev-env.sh  (exports ES_*, REDIS_*, ACME_ARTEMIS_*, etc.)
# ELASTIC_PASSWORD=changeme
# ARTEMIS_USER=artemis
# ARTEMIS_PASSWORD=artemis
